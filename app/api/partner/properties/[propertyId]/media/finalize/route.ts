import { FieldValue } from "firebase-admin/firestore";
import { z } from "zod";
import { getAuthenticatedUser } from "@/lib/auth/session";
import { adminDb } from "@/lib/firebase/admin";
import { propertyOwner } from "@/lib/partner/service";
import { createR2ReadUrl, verifyR2Object } from "@/lib/r2";
const schema = z.object({ uploadId: z.string().uuid() }).strict();
export async function POST(request: Request, { params }: RouteContext<"/api/partner/properties/[propertyId]/media/finalize">) { const user = await getAuthenticatedUser(); if (!user) return Response.json({ error: "Unauthenticated." }, { status: 401 }); try { const { propertyId } = await params; const property = await propertyOwner(user.uid, propertyId); const { uploadId } = schema.parse(await request.json()); const upload = await adminDb.collection("pendingUploads").doc(uploadId).get(); const data = upload.data(); if (!data || data.ownerId !== user.uid || data.propertyId !== propertyId || data.kind !== "property_image" || data.expiresAt < Date.now()) throw new Error("UPLOAD_EXPIRED"); await verifyR2Object(data.objectKey, data.sizeBytes, data.checksum); const asset = adminDb.collection("mediaAssets").doc(); await asset.set({ propertyId, ownerId: user.uid, kind: "property_image", category: data.category, fileName: data.fileName, mimeType: data.mimeType, sizeBytes: data.sizeBytes, checksum: data.checksum, r2ObjectKey: data.objectKey, isPrivate: true, moderationStatus: "pending", status: "pending", createdAt: FieldValue.serverTimestamp(), createdBy: user.uid, updatedAt: FieldValue.serverTimestamp(), updatedBy: user.uid }); const current = await property.get(); const hadCover = Boolean(current.data()?.coverMediaId); await property.update({ mediaIds: FieldValue.arrayUnion(asset.id), ...(hadCover ? {} : { coverMediaId: asset.id }), updatedAt: FieldValue.serverTimestamp(), updatedBy: user.uid }); await upload.ref.delete(); let imageUrl: string | null = null; try { imageUrl = createR2ReadUrl(data.objectKey).url; } catch { imageUrl = null; } return Response.json({ mediaId: asset.id, media: { id: asset.id, kind: "property_image", category: data.category ?? null, fileName: data.fileName ?? null, altText: "", moderationStatus: "pending", isCover: !hadCover, imageUrl }, coverMediaId: hadCover ? current.data()?.coverMediaId : asset.id }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Unable to finalize upload." }, { status: 422 }); } }
